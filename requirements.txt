# Core dependencies for all layers
# Layer A — Conversion
azure-ai-documentintelligence>=1.0.0
azure-ai-formrecognizer>=3.3.0
azure-identity>=1.16.0
docling>=2.0.0
pydantic-settings>=2.0.0

# Layer B — Structure-aware (no additional deps beyond stdlib)

# Layer C — Chunk + Embed
FlagEmbedding>=1.2.0
openai>=1.0.0

# Layer D — Retrieval & Index
qdrant-client>=1.9.0

# Layer E — Generation
# (LLM clients configured via env vars)

# Dev / Test
pytest>=8.0.0
numpy>=1.26.0
